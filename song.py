import os
import pywhatkit

def son(musicname, music_dir, music_dir2, songs, songs2):
    if "revenge" in musicname:
        os.startfile(os.path.join(music_dir, songs[0]))
    elif "teeth" in musicname:
        os.startfile(os.path.join(music_dir, songs[1]))
    elif "alone" in musicname:
        os.startfile(os.path.join(music_dir, songs[2]))
    elif "dark side" in musicname:
        os.startfile(os.path.join(music_dir, songs[3]))
    elif "faded" in musicname:
        os.startfile(os.path.join(music_dir, songs[4]))
    elif "lalala" in musicname or "la la la" in musicname:
        os.startfile(os.path.join(music_dir, songs[5]))
    elif "danger" in musicname:
        os.startfile(os.path.join(music_dir, songs[6]))
    elif "on and on" in musicname:
        os.startfile(os.path.join(music_dir, songs[7]))
    elif "still" in musicname:
        os.startfile(os.path.join(music_dir, songs[8]))
    elif "godzilla" in musicname:
        os.startfile(os.path.join(music_dir, songs[9]))
    elif "lose yourself" in musicname or "Lose Yourself" in musicname:
        os.startfile(os.path.join(music_dir, songs[10]))
    elif "god" in musicname:
        os.startfile(os.path.join(music_dir, songs[11]))
    elif "venom" in musicname:
        os.startfile(os.path.join(music_dir, songs[12]))
    elif "paradise" in musicname:
        os.startfile(os.path.join(music_dir, songs[13]))
    elif "believer" in musicname:
        os.startfile(os.path.join(music_dir, songs[14]))
    elif "natural" in musicname:
        os.startfile(os.path.join(music_dir, songs[15]))
    elif "radioactive" in musicname:
        os.startfile(os.path.join(music_dir, songs[16]))
    elif "warriors" in musicname:
        os.startfile(os.path.join(music_dir, songs[17]))
    elif "industry baby" in musicname:
        os.startfile(os.path.join(music_dir, songs[18]))
    elif "heroes tonight" in musicname:
        os.startfile(os.path.join(music_dir, songs[19]))
    elif "legends" in musicname:
        os.startfile(os.path.join(music_dir, songs[20]))
    elif "montero" in musicname or "monteiro" in musicname:
        os.startfile(os.path.join(music_dir, songs[21]))
    elif "old town road" in musicname:
        os.startfile(os.path.join(music_dir, songs[22]))
    elif "everyday" in musicname:
        os.startfile(os.path.join(music_dir, songs[23]))
    elif "happier" in musicname:
        os.startfile(os.path.join(music_dir, songs[24]))
    elif "one thing right" in musicname:
        os.startfile(os.path.join(music_dir, songs[25]))
    elif "pig step remix" in musicname:
        os.startfile(os.path.join(music_dir, songs[26]))
    elif "My war" in musicname:
        os.startfile(os.path.join(music_dir, songs[27]))
    elif "wanna be your slave" in musicname or "want to be a Slave" in musicname:
        os.startfile(os.path.join(music_dir, songs[28]))
    elif "clouds" in musicname:
        os.startfile(os.path.join(music_dir, songs[29]))
    elif "search" in musicname:
        os.startfile(os.path.join(music_dir, songs[30]))
    elif "pig step" in musicname:
        os.startfile(os.path.join(music_dir, songs[31]))
    elif "one right now" in musicname:
        os.startfile(os.path.join(music_dir, songs[32]))
    elif "rise" in musicname:
        os.startfile(os.path.join(music_dir, songs[33]))
    elif "see me fall" in musicname:
        os.startfile(os.path.join(music_dir, songs[34]))
    elif "fire" in musicname:
        os.startfile(os.path.join(music_dir, songs[35]))
    elif "soviet connection" in musicname:
        os.startfile(os.path.join(music_dir, songs[36]))
    elif "cradles" in musicname:
        os.startfile(os.path.join(music_dir, songs[37]))
    elif "pain" in musicname:
        os.startfile(os.path.join(music_dir, songs[38]))
    elif "born" in musicname:
        os.startfile(os.path.join(music_dir, songs[39]))
    elif "tears" in musicname:
        os.startfile(os.path.join(music_dir, songs[40]))
    elif "blinding" in musicname:
        os.startfile(os.path.join(music_dir, songs[41]))
    elif "star boy" in musicname or "starboy" in musicname:
        os.startfile(os.path.join(music_dir, songs[42]))
    elif "fly away" in musicname:
        os.startfile(os.path.join(music_dir, songs[43]))
    elif "unity" in musicname:
        os.startfile(os.path.join(music_dir, songs[44]))
    elif "genesis" in musicname:
        os.startfile(os.path.join(music_dir, songs[45]))
    elif "chlorine" in musicname:
        os.startfile(os.path.join(music_dir, songs[46]))
    elif "heathens" in musicname or "hidden" in musicname:
        os.startfile(os.path.join(music_dir, songs[47]))
    elif "black" in musicname:
        os.startfile(os.path.join(music_dir, songs[48]))
    elif "young" in musicname:
        os.startfile(os.path.join(music_dir2, songs2[0]))
    elif "bang" in musicname:
        os.startfile(os.path.join(music_dir2, songs2[1]))
    elif "house" in musicname:
        os.startfile(os.path.join(music_dir2, songs2[2]))
    elif "ashes" in musicname:
        os.startfile(os.path.join(music_dir2, songs2[3]))
    elif "lovely" in musicname:
        os.startfile(os.path.join(music_dir2, songs2[4]))
    elif "light switch" in musicname:
        os.startfile(os.path.join(music_dir2, songs2[5]))
    elif "arcade" in musicname:
        os.startfile(os.path.join(music_dir2, songs2[6]))
    elif "enemy" in musicname and "imagine" in musicname:
        os.startfile(os.path.join(music_dir2, songs2[10]))
    elif "enemy" in musicname:
        os.startfile(os.path.join(music_dir2, songs2[7]))
    elif "blood" in musicname:
        os.startfile(os.path.join(music_dir2, songs2[8]))
    elif "rain" in musicname:
        os.startfile(os.path.join(music_dir2, songs2[9]))
    elif "astronaut" in musicname:
        os.startfile(os.path.join(music_dir2, songs2[11]))
    elif "dead" in musicname:
        os.startfile(os.path.join(music_dir2, songs2[12]))
    elif "circulation" in musicname:
        os.startfile(os.path.join(music_dir2, songs2[13]))
    elif "navigator" in musicname:
        os.startfile(os.path.join(music_dir2, songs2[14]))
    elif "inferno" in musicname:
        os.startfile(os.path.join(music_dir2, songs2[15]))
    elif "stay" in musicname:
        os.startfile(os.path.join(music_dir2, songs2[16]))
    elif "fake" in musicname:
        os.startfile(os.path.join(music_dir2, songs2[17]))
    elif "feel my face" in musicname:
        os.startfile(os.path.join(music_dir2, songs2[18]))
    elif "unravel" in musicname:
        os.startfile(os.path.join(music_dir2, songs2[19]))
    else:
        pywhatkit.playonyt(musicname)
def roast(rand):
    if (rand == 1):
        print("you are as useless as the ueue in queue")
        return("you are as useless as the ueue in queue")
    elif (rand == 2):
        print("if laughter is the best medicine the ur face must be curing the world")
        return("if laughter is the best medicine the ur face must be curing the world")
    elif (rand == 3):
        print("if I wanted to kill myself, I would jump from your ego down to your iq")
        return("if I wanted to kill myself, I would jump from your ego down to your iq")
    elif (rand == 4):
        print("When  see your face I wont change a single thing, except the direction I was walking in")
        return("When  see your face I wont change a single thing, except the direction I was walking in")
    elif (rand == 5):
        print("When you were born, the doctor there you out of the window but the window threw you back in")
        return("When you were born, the doctor there you out of the window but the window threw you back in")
    elif (rand == 6):
        print("light travels faster than sound, which is why you seemed bright until you spoke")
        return("light travels faster than sound, which is why you seemed bright until you spoke")
    elif (rand == 7):
        print("your face makes onions cry")
        return("your face makes onions cry")
    elif (rand == 8):
        print("you have only got 3 braincells left and they are all fighting for attention")
        return("you have only got 3 braincells left and they are all fighting for attention")
    elif (rand == 9):
        print("You are more disappointing than an unsalted pretzel")
        return("You are more disappointing than an unsalted pretzel")
    elif (rand == 10):
        print("ur mom gae")
        return("ur mom gae")
    elif (rand == 11):
        print("deforestation became legal after they saw ur mom's bush")
        return ("deforestation became legal after they saw ur mom's bush")
    elif (rand == 12):
        print("mirrors can' talk, you are lucky they can't laugh either")
        return ("mirrors can' talk, you are lucky they can't laugh either")