# Packages the utility commands in commands.py depend on.
# Install into the assistant venv:
#   .\venv\Scripts\python.exe -m pip install -r requirements-commands.txt
#
# Versions are pinned to what was installed and verified working.
# Each command degrades on its own if its package is absent, so this file is
# optional; without it only the stdlib commands (open google / open youtube /
# the time / calculate / search) are available.

PyAutoGUI==0.9.54        # mute, volume up/down
pyjokes==0.8.3           # joke
speedtest-cli==2.1.3     # speed test
wolframalpha==5.1.3      # temperature (also needs a WOLFRAM_API_KEY env var)
pywhatkit==5.4           # youtube autoplay, whatsapp messaging, roasts (song.py)
keyboard==0.13.5         # whatsapp messaging (Whatsapp.py)

# Only a fallback now. Wikipedia lookups go through the REST summary endpoint
# in commands.py, because this package was returning non-JSON for every query.
wikipedia==1.4.0
